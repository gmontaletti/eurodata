% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{ed_transform}
\alias{ed_transform}
\title{Transform and export Eurostat datasets into the eurodata unified schema}
\usage{
ed_transform(data_list, specs = eurodata::dataset_specs)
}
\arguments{
\item{data_list}{A named list of data.frames or data.tables, as returned
by the collection step (e.g. \code{readRDS("EURO_indicatori.rds")}). Names
must correspond to Eurostat dataset identifiers.}

\item{specs}{A list of spec lists describing the transformations. Defaults
to \code{eurodata::dataset_specs} defined in \code{R/config.R}.}
}
\value{
A \code{data.table} with columns:
\describe{
\item{iCode}{Composite indicator code (iCode + abbreviated gruppo).}
\item{iName}{Human-readable indicator name (indice + gruppo).}
\item{uCode}{NUTS-2 geo code.}
\item{uName}{Region label (paese + regione or geo).}
\item{TIME_PERIOD}{Reference year.}
\item{values}{Numeric indicator value.}
}
}
\description{
Exported functions for applying dataset specs to raw Eurostat data and
writing the results to disk.
Transform raw Eurostat datasets into a unified indicator table
}
\details{
Iterates over the transformation specifications in \code{specs}, looks up each
corresponding dataset in \code{data_list}, applies the full transformation
pipeline (filtering, remapping, aggregation), and combines all results
into a single \code{data.table} with the eurodata schema.
}
\examples{
\dontrun{
raw <- readRDS("../datasets/EURO_indicatori.rds")
unified <- ed_transform(raw)
}

}
