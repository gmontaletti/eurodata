% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peergroup.R
\name{ed_peergroup}
\alias{ed_peergroup}
\title{Select a peer group of regions comparable to a reference region}
\usage{
ed_peergroup(
  dt,
  reference_region = "IT Veneto",
  year = 2022,
  pop_icode = "Popolaziototal",
  pps_icode = "PPSprcptPpurchasing",
  pop_ratio = 0.3,
  pps_low_ratio = 0.67,
  pps_upper = 200,
  target_year = 2024,
  exclude_incomplete = TRUE,
  min_nonmissing = NULL
)
}
\arguments{
\item{dt}{A \code{data.table} with the unified eurodata schema. Expected
columns: \code{iCode}, \code{iName}, \code{uCode}, \code{uName}, \code{TIME_PERIOD}, \code{values}.
Typically the output of \code{\link[=ed_transform]{ed_transform()}}.}

\item{reference_region}{Character string identifying the reference region
in the \code{uName} column. Defaults to \code{"IT Veneto"}.}

\item{year}{Integer. The year used to compute PPS and population for the
peer group comparison. Defaults to \code{2022}.}

\item{pop_icode}{Character string. The \code{iCode} value that identifies
the population indicator. Defaults to \code{"Popolaziototal"}.}

\item{pps_icode}{Character string. The \code{iCode} value that identifies
the PPS per capita indicator. Defaults to \code{"PPSprcptPpurchasing"}.}

\item{pop_ratio}{Numeric between 0 and 1. Minimum ratio of the reference
region's population that a peer region must exceed. Defaults to \code{0.3}.}

\item{pps_low_ratio}{Numeric between 0 and 1. Lower bound for PPS
expressed as a fraction of the reference region's PPS. Defaults to \code{0.67}.}

\item{pps_upper}{Numeric. Absolute upper bound for PPS. Regions with PPS
above this value are excluded. Defaults to \code{200}.}

\item{target_year}{Integer. The year for which the final indicator data
is returned. Defaults to \code{2024}.}

\item{exclude_incomplete}{Logical. If \code{TRUE}, indicators with fewer
non-missing observations than \code{min_nonmissing} are dropped. Defaults to
\code{TRUE}.}

\item{min_nonmissing}{Integer or \code{NULL}. Minimum number of non-missing
values an indicator must have to be retained. If \code{NULL} (the default),
the threshold is set to half the number of regions in the peer group.}
}
\value{
A \code{data.table} with the same columns as \code{dt}, filtered to the peer
group regions for the target year. Returned invisibly.
}
\description{
Identifies NUTS-2 regions that are comparable to a reference region based
on population size and GDP per capita in purchasing power standards (PPS).
The function filters the full indicator dataset to keep only the selected
peer group for the target year, and optionally removes indicators that have
too many missing values.
}
\details{
The peer group selection proceeds as follows:
\enumerate{
\item Extract population and PPS values for the comparison year.
\item Pivot to wide format so each region has one PPS and one population value.
\item Retrieve the reference region's PPS and population.
\item Keep regions where population exceeds a fraction of the reference
region's population AND PPS falls between a lower bound and an absolute
upper bound.
\item Filter the original dataset to the peer group regions and the target year.
\item Optionally drop indicators with insufficient non-missing observations.
}
}
\examples{
\dontrun{
peer <- ed_peergroup(pgr)

peer <- ed_peergroup(
  pgr,
  reference_region = "DE Bayern",
  pop_ratio = 0.5,
  pps_low_ratio = 0.75,
  target_year = 2023
)
}
}
